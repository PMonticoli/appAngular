import { ProbandoDirective } from './probando.directive';

describe('ProbandoDirective', () => {
  it('should create an instance', () => {
    const directive = new ProbandoDirective();
    expect(directive).toBeTruthy();
  });
});
