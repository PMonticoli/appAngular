import { DescuentoMayoristaPipe } from './descuento-mayorista.pipe';

describe('DescuentoMayoristaPipe', () => {
  it('create an instance', () => {
    const pipe = new DescuentoMayoristaPipe();
    expect(pipe).toBeTruthy();
  });
});
