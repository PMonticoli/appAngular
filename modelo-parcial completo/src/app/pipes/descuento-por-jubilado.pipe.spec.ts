import { DescuentoPorJubiladoPipe } from './descuento-por-jubilado.pipe';

describe('DescuentoPorJubiladoPipe', () => {
  it('create an instance', () => {
    const pipe = new DescuentoPorJubiladoPipe();
    expect(pipe).toBeTruthy();
  });
});
