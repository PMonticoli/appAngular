import { TotalConAlicuotaPipe } from './total-con-alicuota.pipe';

describe('TotalConAlicuotaPipe', () => {
    it('create an instance', () => {
        const pipe = new TotalConAlicuotaPipe();
        expect(pipe).toBeTruthy();
    });
});
